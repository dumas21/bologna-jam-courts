
import { Playground, PlaygroundComment, CheckInRecord, RegisteredUser, WeatherData, PlaygroundFilters, PlaygroundEvent } from "@/types/playgroundTypes";

// Re-export all the types from playgroundTypes.ts
export type { Playground, PlaygroundComment as Comment, CheckInRecord, RegisteredUser, WeatherData, PlaygroundFilters, PlaygroundEvent };
